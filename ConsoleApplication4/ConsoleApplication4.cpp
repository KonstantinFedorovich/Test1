#include <iostream>
#include <cmath>
#include <algorithm>

using namespace std;
int main() {
	setlocale(LC_ALL, "RU");
	int arr[] = { 2,8,16 };
	for (int i = 0; i < 3; i++)
		cout << arr[i] <<" ";
	//изменение
	//int n;
	//cin >> n;
	//for (int i = 1; i <= n; i += 1) {
	//	cout << "Счетчик равен " << i << endl;
	return 0;
}
	
